package at.jku.sea.cloud.listeners;

/**
 * Event source for events receivable by the Listener interface L.
 * @author jMayer
 *
 * @param <L> Listener interface able to receive events from this EventSource.
 */
public interface EventSource<L> {
  void addListener(final L l);
  void removeListener(final L l);
}
